# docker-compose.yml
version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: server/dockerfile.yml
    image: tbfs-backend
    ports:
      - "8000:8000"
    networks:
      - tbfs_net
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  frontend:
    build:
      context: .
      dockerfile: gui/dockerfile.yml
    image: tbfs-frontend
    environment:
      - API_URL=http://backend:8000
    networks:
      - tbfs_net
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

networks:
  tbfs_net:
    driver: overlay
